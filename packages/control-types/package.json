{
  "name": "@hearthold/control-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Hearthold control-API payload types — the wire contract shared by the Node agent daemons and the browser GUIs. Pure types, no runtime, no Node imports.",
  "exports": {
    ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" }
  },
  "types": "./dist/index.d.ts",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build"
  }
}
