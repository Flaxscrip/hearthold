# Sevenfold P0 Brief — moved

The canonical brief now lives in the **sevenfold repo**: `Sevenfold/P0-BRIEF.md`.

What remains hearthold's concern from that brief:
- **Ruleset chain primitives** (upstream PR): `Ruleset` type + `signRuleset()` via `addProof` + `verifyRulesetChain()` — generalizes the Sovereign-signed policy already on this roadmap. On-node schema: `did:cid:bagaaieraarhpba77hdfakhn3remyvj77kciw3jtlmvjsufsgcoj5zpldqruq` (alias `sevenfold-ruleset-schema`).
- **Package publishing**: `@hearthold/core` + `@hearthold/control-types` consumable from outside the monorepo.

Design canon: `../game-of-life/Sevenfold-Concept.md`, `Sevenfold-Architecture.md`, `Keeper-Schema-Mapping.md`.
